import org.junit.Test;

public class GeneratorTest {
    /**
     * We have this super simple test to assert that the generators were
     * compiled correctly.
     */
    @Test
    public void testGeneratorsInstantiation() {
        // TODO: Fix this test, as it somehow passes when pom changes should break it
        new Generator();
    }
}
